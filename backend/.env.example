# Application Settings
APP_NAME="AI Business Platform"
DEBUG=true
API_V1_PREFIX="/api/v1"

# Server Settings
HOST="0.0.0.0"
PORT=9000

# CORS Settings (comma-separated for multiple values)
CORS_ORIGINS=http://localhost:3000
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=*
CORS_ALLOW_HEADERS=*

# Email Settings
EMAIL_ENABLED=false
EMAIL_PROVIDER=mock
EMAIL_FROM=noreply@aibusiness.platform
EMAIL_FROM_NAME="AI Business Platform"

# Security Settings
SECRET_KEY=your_secret_key_here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Database Settings
DATABASE_URL=sqlite:///./test.db
